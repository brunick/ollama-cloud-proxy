services:
  ollama-cloud-proxy:
    build: .
    ports:
      - "11434:11434"
    environment:
      - ALLOW_UNAUTHENTICATED_ACCESS=${ALLOW_UNAUTHENTICATED_ACCESS:-true}
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    restart: always
